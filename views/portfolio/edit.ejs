<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Stock - Stock Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/portfolio/dashboard">Stock Tracker</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/portfolio/dashboard">Dashboard</a>
                <a class="nav-link" href="/auth/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h2>Edit Stock: <%= stock.symbol %></h2>
        <form action="/portfolio/edit/<%= stock._id %>" method="POST">
            <div class="mb-3">
                <label class="form-label">Quantity</label>
                <input type="number" class="form-control" name="quantity" value="<%= stock.quantity %>" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Purchase Price</label>
                <input type="number" step="0.01" class="form-control" name="purchasePrice" value="<%= stock.purchasePrice %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Update Stock</button>
            <a href="/portfolio/dashboard" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</body>
</html>